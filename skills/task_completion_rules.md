---
name: task-completion-rules
description: "タスク完了時のtasks.yaml更新必須ルール"
license: "MIT"
---

# タスク完了時の必須ルール

## 概要

`/aipo/04_deliver` でタスクを完了したら、**必ず** tasks.yaml を更新してください。

---

## 🚨 必須アクション

### 1. tasks.yaml の更新

タスク完了時、以下の項目を必ず更新：

```yaml
tasks:
  - id: "T001"
    name: "フォーム作成"
    status: "completed"        # ← 必須: pending → completed
    completed_at: "2026-01-20" # ← 必須: 完了日を追加
    deliverables:              # ← 必須: 成果物パスを追加
      - "Documents/T001_form.md"
      - "Documents/T001_slack_message.md"
```

### 2. 更新チェックリスト

- [ ] `status`: `pending` → `completed`
- [ ] `completed_at`: 完了日（YYYY-MM-DD形式）
- [ ] `deliverables`: 生成した成果物のパスをリスト

### 3. 進捗表示

更新後、必ず進捗を表示：

```
✅ T001 完了
📊 進捗更新: 1/10 → 2/10 (20%)
📝 tasks.yaml 更新済み
```

---

## ステータス一覧

| ステータス | 意味 | 条件 |
|-----------|------|------|
| `pending` | 未着手 | 依存タスクが完了済み |
| `in_progress` | 進行中 | 作業開始済み |
| `completed` | 完了 | 成果物生成済み |
| `blocked` | ブロック | 依存タスクが未完了 |
| `cancelled` | キャンセル | 実行不要と判断 |

---

## 複数タスク完了時

並列実行で複数タスクが完了した場合、まとめて更新：

```yaml
tasks:
  - id: "T001"
    status: "completed"
    completed_at: "2026-01-20"
    deliverables:
      - "Documents/T001_form.md"
  
  - id: "T002"
    status: "completed"
    completed_at: "2026-01-20"
    deliverables:
      - "Documents/T002_venue.md"
```

---

## 依存タスクの解除

タスク完了により、依存していたタスクの `blocked` を解除：

```
T001 完了 → T003 の blocked 解除 → pending に変更
```

---

## AI への指示

**04_deliver 実行時、必ず以下を行うこと：**

1. タスク実行完了後、tasks.yaml を読み込む
2. 該当タスクの status を `completed` に更新
3. `completed_at` に完了日を追加
4. `deliverables` に成果物パスを追加
5. 依存タスクの blocked を解除
6. 進捗サマリーを表示
7. 次に実行可能なタスクを案内

**省略禁止**: tasks.yaml の更新を省略してはならない
